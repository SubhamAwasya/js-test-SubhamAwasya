const decodeTheRing = function (s, p) {
  // write your code here

  let sl = s.length;
  let pl = p.length;

  while (sl > 0 && pl > 0) {
    sl--;
  }

  console.log("======" + s[1]);
};

module.exports = decodeTheRing;
